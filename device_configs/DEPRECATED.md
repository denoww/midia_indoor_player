# ⚠️ DESCONTINUADO (19/09/2026)

Este diretório inteiro (`crontab-sc-player`, `lxde-autostart`,
`midia_indoor_reinicio_diario`, `midia_indoor_update_diario`, `panel`,
`player.desktop`, `splash.png`, `wallpaper.png`) é específico do modo de
instalação Linux/Raspberry Pi / Orange Pi (LXDE, cron, boot splash).

Sem manutenção real desde 2023, sem evidência de nenhum device vivo em
produção hoje. O parque atual roda no app Android `corpflix`
(repo `denoww/corpflix`) ou, como frota legada ainda viva, em Windows via
Chrome kiosk (ver `README.md` § "Config windows" — esse SIM continua em
uso, ~30 TVs, e o Chrome dele se autoatualiza sozinho, sem ação
necessária).

Contexto completo da investigação: `ROADMAP_dependencias_criticas.md`
item #26 (repo `seucondominio`).
